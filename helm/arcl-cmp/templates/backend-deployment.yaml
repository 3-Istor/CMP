apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "arcl-cmp.fullname" . }}-backend
  labels:
    {{- include "arcl-cmp.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "arcl-cmp.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: backend
  template:
    metadata:
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secret.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "arcl-cmp.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: backend
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "arcl-cmp.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
      - name: init-db-dir
        image: busybox:latest
        command: ['sh', '-c', 'mkdir -p /app/db && chmod 777 /app/db']
        volumeMounts:
        - name: db-storage
          mountPath: /app/db
      containers:
      - name: backend
        securityContext:
          {{- toYaml .Values.securityContext | nindent 12 }}
        image: "{{ .Values.image.backend.repository }}:{{ .Values.image.backend.tag }}"
        imagePullPolicy: {{ .Values.image.backend.pullPolicy }}
        ports:
        - name: http
          containerPort: 8000
          protocol: TCP
        env:
        {{- range $key, $value := .Values.env.backend }}
        - name: {{ $key }}
          value: {{ $value | quote }}
        {{- end }}
        {{- if .Values.secrets.create }}
        {{- $secret := include "arcl-cmp.fullname" . }}
        {{- range $env, $key := dict
              "OS_AUTH_URL" "os-auth-url"
              "OS_USERNAME" "os-username"
              "OS_PASSWORD" "os-password"
              "OS_PROJECT_NAME" "os-project-name"
              "AWS_ACCESS_KEY_ID" "aws-access-key-id"
              "AWS_SECRET_ACCESS_KEY" "aws-secret-access-key"
              "AWS_DEFAULT_REGION" "aws-region"
              "TF_BACKEND_AWS_ACCESS_KEY_ID" "aws-access-key-id"
              "TF_BACKEND_AWS_SECRET_ACCESS_KEY" "aws-secret-access-key"
              "TF_BACKEND_AWS_REGION" "aws-region"
              "TF_BACKEND_S3_BUCKET" "aws-s3-bucket"
              "TF_BACKEND_S3_KEY_PREFIX" "aws-s3-key-prefix"
              "TF_BACKEND_S3_DYNAMODB_TABLE" "aws-dynamodb-table"
              "DISCORD_WEBHOOK_URL" "discord-webhook-url" }}
        - name: {{ $env }}
          valueFrom:
            secretKeyRef:
              name: {{ $secret }}-secret
              key: {{ $key }}
        {{- end }}
        {{- end }}
        {{- with .Values.extraEnv }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          {{- toYaml .Values.resources.backend | nindent 12 }}
        volumeMounts:
        - name: data
          mountPath: /app/data
        - name: db-storage
          mountPath: /app/db
        - name: logs-storage
          mountPath: /app/logs
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: {{ include "arcl-cmp.fullname" . }}-backend-data
      - name: db-storage
        persistentVolumeClaim:
          claimName: {{ include "arcl-cmp.fullname" . }}-backend-db
      - name: logs-storage
        emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
